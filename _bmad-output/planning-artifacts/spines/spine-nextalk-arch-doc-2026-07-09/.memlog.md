---
scope: docs/architecture.md 棕地校准至 v0.2.8
purpose: build substrate 修正
altitude: system
updated: 2026-07-09T01:17
---

- (decision) 保持 docs/architecture.md 传统文档形态做棕地校准（非重构为 spine）；PRD v1.2 为上游事实来源
- (version) 版本权威=version.yaml: app 0.2.8 / addon 0.3.0；pubspec 由脚本经 --dart-define 同步
- (decision) 并发模型结论回填：流水线在主 Isolate 以异步轮询循环 (Future.delayed poll + 阻塞 read) 运行，未启用后台 Isolate（原文档的'掉帧再迁移'预案未触发）
- (decision) IPC 实际约束记载：MAX_MESSAGE_SIZE=1MB、recv 超时 30s+零字节探活、顺序处理单客户端；已知限制：无协议版本号/无ACK
- (decision) 模型校验现状：zipformer int8 有 SHA256；sensevoice/silero_vad/zipformer standard 官方未发布校验值→跳过（记为已知限制）
- (decision) 安全已知限制：XDG_RUNTIME_DIR 未设置时回退 /tmp/nextalk-fcitx5.sock（可预测路径，chmod 600 不能防抢占）
- (decision) 组件设计新增：ASR 引擎抽象层 (ASREngine 接口 + factory + initializer + zipformer/sensevoice 双实现)；音频三层 AudioCapture 门面→PulseAudioCapture 主→PortAudio 回退；commitText 用 mostRecentInputContext 无焦点锁定
- (change) 评审修复[critical]：silero VAD 仅 SenseVoice 路径使用，Zipformer 用内置端点规则 rule1/2/3——修正 §2.1 图/§3 表/§4.2.2，并连带修正 PRD FR3（两份）
- (change) 评审修复[critical]：协议实有 1 字节 ACK（客户端不消费）——§4.1.1 改述为'无版本号/魔数；ACK 无端到端确认语义'
- (change) 评审修复[high]：模型表合并 Zipformer 为单压缩包（含双版本权重，下载即 SHA256 校验）；custom_url 仅 Zipformer 生效；§5.1 记载 build-pkg.sh 缺 --dart-define 的'dev'版本缺口；bundle 补 libportaudio.so.2；commitText 补无上下文遍历回退
- (event) architecture docs v2.1 finalized (docs/architecture.md + architecture_zh.md)
